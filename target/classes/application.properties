# ZPMC Container Swap System Configuration

# Maximum containers in RAM cache
cache.max-capacity=5

# Directory where swapped containers are stored as JSON files
swap.directory=./swap-storage

# Server port
server.port=8080
